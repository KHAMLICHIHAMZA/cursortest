# Copier vers `.env` à la racine de mobile-agent (même dossier que package.json).
# Redémarrer le Metro bundler après modification (`r` dans le terminal Expo).
#
# Préprod / staging : URL de ton API (Render, etc.), avec ou sans suffixe /api/v1
EXPO_PUBLIC_API_URL=https://votre-api-preprod.example.com
